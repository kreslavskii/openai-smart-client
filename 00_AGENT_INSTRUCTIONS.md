# ⚠️ ВАЖНО ДЛЯ АГЕНТА: Начните отсюда

> **ЭТОТ ФАЙЛ — ПЕРВАЯ ТОЧКА ВХОДА ДЛЯ AI АГЕНТОВ**  
> **ЧИТАЙТЕ ЭТОТ ФАЙЛ ПЕРВЫМ ПЕРЕД РАБОТОЙ С МОДУЛЕМ**

## Для агента: Как использовать этот модуль

1. **Прочитайте этот файл полностью** — здесь вся необходимая информация для подключения и использования модуля
2. **Не анализируйте исходный код** — все сигнатуры функций, типы данных и примеры уже описаны здесь
3. **Следуйте инструкциям по шагам** — от проверки зависимостей до примеров использования

## Полная документация

**Основной файл документации:** [`OPENAI_CLIENT_DOC.md`](./OPENAI_CLIENT_DOC.md)

Этот файл содержит:
- ✅ Пошаговые инструкции по подключению модуля
- ✅ Полные сигнатуры всех публичных функций
- ✅ Описание всех типов данных
- ✅ Примеры использования для каждого сценария
- ✅ Информацию об обработке ошибок
- ✅ Справочники моделей и конфигурацию

## Быстрый старт для агента

```python
# 1. Проверка зависимостей
import sys
if sys.version_info < (3, 10):
    raise RuntimeError("Требуется Python 3.10+")

# 2. Установка зависимостей (если необходимо)
# pip install openai pyyaml

# 3. Импорт модуля
from openai_client_module import call_openai

# 4. Проверка API ключа
import os
if not os.getenv("OPENAI_API_KEY"):
    raise ValueError("OPENAI_API_KEY не установлен")

# 5. Использование
result = call_openai("Привет!")
print(result)
```

## Структура модуля

```
openai_client_module/
├── 00_AGENT_INSTRUCTIONS.md  ← ВЫ ЗДЕСЬ (читайте первым)
├── OPENAI_CLIENT_DOC.md      ← Полная документация (читайте вторым)
├── README.md                  ← Для пользователей (опционально)
├── __init__.py                ← Реэкспорт API
├── openai_types.py           ← Типы и справочники
├── openai_config.py          ← Конфигурация
├── openai_client.py          ← Класс OpenAIClient
└── openai_api.py             ← Функциональный API
```

## Что дальше?

1. Откройте [`OPENAI_CLIENT_DOC.md`](./OPENAI_CLIENT_DOC.md) для получения полной информации
2. Используйте раздел "Публичный API: Сигнатуры функций" для генерации вызовов
3. Смотрите примеры в разделе "Примеры использования для агентов"

---

**Версия модуля:** 1.0.0 | **Python:** 3.10+ | **Зависимости:** openai, pyyaml

